services:
  - type: web
    name: tiranga-colour-trading
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        value: postgresql://postgres:tiranga-db123@db.hooqiccbtakpcogziona.supabase.co:5432/postgres
